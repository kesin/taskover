#color_tag_field.col-md-4.col-md-offset-3
  .navbar-form.navbar-left.color_tag_filed
    = link_to request.path, class: 'reset-icon', title: '重置分类' do
      %i.icon-circle-blank
    - Plan::COLOR_TAG.each do |key, value|
      = link_to "#{request.path}?colortag=#{value}", class:"color-tag-#{key}" do
        %i.icon-circle